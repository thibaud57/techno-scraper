{
  "mcpServers": {
    "techno-scraper": {
      "command": "python",
      "args": [
        "-m",
        "app.mcp.server"
      ],
      "cwd": "/path/to/techno-scraper"
    }
  }
}

// NOTE: Les variables d'environnement (SOUNDCLOUD_CLIENT_ID, SOUNDCLOUD_CLIENT_SECRET)
// sont lues automatiquement depuis le fichier .env dans le répertoire du projet.
//
// Si vous n'avez pas de .env (ex: environnement isolé), ajoutez la section "env" :
// "env": {
//   "SOUNDCLOUD_CLIENT_ID": "your-soundcloud-client-id",
//   "SOUNDCLOUD_CLIENT_SECRET": "your-soundcloud-client-secret"
// }
